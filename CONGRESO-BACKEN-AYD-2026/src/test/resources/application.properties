# Test properties to satisfy @Value placeholders used by services
aws.s3.bucketName=test-bucket
aws.accessKeyId=test-access-key
aws.secretKey=test-secret-key
aws.s3.region=us-east-1

# JWT properties used by JwtService
security.jwt.secret-key=ZmFrZXNlY3JldA==
security.jwt.expiration-time=3600000

# Mail (won't be used because JavaMailSender is mocked in tests)
spring.mail.host=localhost
spring.mail.port=25

# Disable Flyway during tests to avoid running Postgres-specific migrations against H2
spring.flyway.enabled=false
